DSB — Final bundle (single-folder).

Files included:
- index.html (single file with inline CSS & JS)
- payments.html (private payments page)
- style.css (optional copy)
- assets/ (SVG images used by the site)

How to use:
1. Upload the whole folder to your GitHub repository root (or merge files into your existing repo).
2. Make sure GitHub Pages is enabled for branch 'main' and folder '/'.
3. Visit https://<your-username>.github.io/<repo-name>/
